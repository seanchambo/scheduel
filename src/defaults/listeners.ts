import { ListenersConfig } from '../models';

const listeners: ListenersConfig = {
  assignments: {
    drag: () => { },
    drop: () => { },
  },
  external: {
    drag: () => { },
    drop: () => { },
  }
}

export default listeners